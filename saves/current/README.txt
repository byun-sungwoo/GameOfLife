saves/current/current.life is a file that gets generated when you enter the pause menu's load file state.
This is just so that when hovering over files in the pause menu, it shows
what's inside the file, but you can still return to your current run.

